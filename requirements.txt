# 钉死版本，不用 >= 。
#
# 理由：这个项目会在两台机器上装（你的电脑、云服务器），装的时间还差了好几天。
# 用 >= 的话两边解析出来的版本可能不一样，于是"本地跑得好好的"变成一句空话——
# 排查解析出错时你根本分不清是教务处改了页面，还是 lxml 换了行为。
#
# 想升级：手动改这里的版本号，在测试环境验证过再部署。
# 装不上时不要在生产机临时放宽版本；先在同版本 Linux 环境更新并测试依赖锁。
#
# requirements.lock.txt 是在 Linux 上解析出来的完整传递依赖锁，
# setup.sh 会优先安装它；本文件是人工升级的入口。

requests==2.33.0
PyYAML==6.0.3
beautifulsoup4==4.15.0
lxml==6.1.1
# 教务系统登录密码要做 AES 加密
pycryptodome==3.23.0
# 把密码存进系统密钥链，免得明文躺在 config.yaml 里。
# Linux 上无头环境没有可用后端时会自动退回环境变量，不影响运行。
keyring==25.7.0
