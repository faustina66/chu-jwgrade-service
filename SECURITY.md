# 安全说明

## 使用者注意事项

- 不要提交 `config.yaml`、`.env`、`data/`、日志、Cookie、CAS ticket、PushPlus token、服务器密钥或完整成绩页面。
- 不要在 Issue、Pull Request、截图或聊天记录中粘贴密码、token、Cookie、完整 URL 或未脱敏成绩。
- 如果密码、Cookie 或 PushPlus token 泄露，应立即在对应服务中撤销或更换；删除文件不能撤销已经泄露的凭据。
- 这是个人自部署程序，不建议用一台公共服务器替多个用户保存统一身份认证凭据。

## 报告问题

请先确认问题不包含真实凭据或个人信息，再通过 GitHub 的私密安全报告渠道提交。普通 Issue 只适合提交已脱敏的复现步骤、版本和错误信息。

安全问题可能涉及学校账号和第三方推送服务；在公开讨论前，应先完成凭据轮换和风险隔离。
